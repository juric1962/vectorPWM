11 10 2018

в функции proc_option( )
после вызовов функции lock_it() поставил return(0)

в функции base1
 analiz_ppp_rx(); 
 if (ResetTaskSim800) return;  // произошел сбой
после вызовов функции lock_it() поставил return


unsigned char at_com_scen_stm_dc_1(unsigned char *cnt,unsigned char *rp)
 case cATH:
          
   //  if(fl_at_com.ok==1){(*cnt)++;*rp=0;fl_at_com.tx_en=1;}
                
         if(fl_at_com.ok==1){event_modem=EVM_AT_OK;return(1);}
         
         // сомнительная вещь ее нужно делать безвсяких условий иначе появляется неопределенность
           if((fl_at_com.err==1)||(fl_at_com.tm_out==1))
           {
           (*rp)++;
           if(*rp>=6){event_modem=EVM_AT_ERR;return(2);}
           fl_at_com.tx_en=1;
           }
когда доходит до этого места посылает только одну команду ATH
  

17 10 18  
обнаружил что RTS CTS препутаны на плате GSM.
чтобы не портить ничего, на процессорной плате RTS CTS ноги переназначил              

22 10 18
добавил GSM поля в настройках
для установки номера устройства необходимо заполнить поле  NUM_contr=0x55aa(21930d)
надо писать два регистра подряд

поставил вывод в 485 при ошибке по порту uart3 -sim800 для контроля 

23 10 2018
при обращении к модбас регистрам через GSM модуль, адрес модбас
не имеет значения
fpp3 при обращении по внутреннему порту взводится флажок zapr_sim800
соответственно в 
modbus.c

модбас адресация не анализируется
можно обращаться как по модбасу так и по еламу
modbusdeviceMonitorприсваивается адрес объекта 2295( фиктивны елам)
потому что она принципаально работает по еламу
ей не нужно знать реальный адрес контроллера

можно писать и читать реальный модбас адрес котроллера

05 11 18
в файле archiv.c
убрал таблицу перекодировки входов
все входы привязаны напрямую
надо после проверки выкинуть из шаблона

12 11 18

с проекта 3.0 перекинул все файлы
аснхронная передача поддерживается
фиктивный елам пока не менял в модбас.с
